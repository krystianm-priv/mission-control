{
	"name": "@mission-control/adapter-sqlite",
	"version": "1.0.0-rc.1",
	"description": "SQLite-backed durable adapter for Mission Control.",
	"type": "module",
	"license": "MIT",
	"engines": {
		"node": ">=24"
	},
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"files": [
		"src",
		"!src/**/*.test.ts",
		"README.md"
	],
	"dependencies": {
		"@mission-control/core": "*"
	},
	"peerDependencies": {
		"typescript": "6.0.2"
	},
	"scripts": {
		"prebuild": "git clean -fdX",
		"build": "tsc -p tsconfig.build.json"
	}
}
