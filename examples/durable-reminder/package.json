{
	"name": "durable-reminder",
	"version": "1.0.0-rc.1",
	"private": true,
	"type": "module",
	"scripts": {
		"check-types": "tsc -p tsconfig.json --noEmit",
		"lint": "biome check .",
		"test": "node --test",
		"dev": "node --experimental-sqlite --watch src/index.ts"
	},
	"dependencies": {
		"@mission-control/core": "workspace:*",
		"@mission-control/sqlite-commander": "workspace:*",
		"zod": "^4.3.6"
	}
}
