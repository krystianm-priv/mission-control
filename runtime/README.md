# @mission-control/runtime

`@mission-control/runtime` provides thin embedded runtime helpers around the core commander API.

It preserves Mission Control's mission terminology and current DSL while giving applications a clearer runtime boundary to start and stop.
